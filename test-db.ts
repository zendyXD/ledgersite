import { createClient } from "@supabase/supabase-js"; import dotenv from "dotenv"; dotenv.config({ path: ".env.local" }); const s = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.SUPABASE_SERVICE_ROLE_KEY!); async function run() { const { data, error } = await s.from("ledger_entries").select("*").limit(5); console.log("ERROR:", error); console.log("DATA:", JSON.stringify(data, null, 2)); } run();
