require('dotenv').config({ path: '.env.local' }); const { createClient } = require('@supabase/supabase-js'); const s = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function run() { console.log('gte/lt:', await s.from('ledger_entries').select('id, entry_date').gte('entry_date', '2026-06-01').lt('entry_date', '2026-07-01').limit(3)); console.log('like:', await s.from('ledger_entries').select('id, entry_date').like('entry_date', '2026-06%').limit(3)); } run();
